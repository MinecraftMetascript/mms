Namespace mms_demo {
    Dimension {
        Sunk = DimensionType()
            .CoordinateScale(16)
            .HasRaids()
            .BedsWork()
            .PiglinSafe().Infiniburn(#b:la).Height(90)
    }


    Surface {
        A = NoiseThreshold(Noise(-5)).Min(5).Max(2)
    }
}
